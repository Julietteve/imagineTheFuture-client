import React from 'react';
import { Header, SocialBar } from '../../components';

const Contact = () => {
    return (
        <>
            <Header/>
            <SocialBar/>
            <h1>CONTACT</h1>
        </>
    );
};

export default Contact;