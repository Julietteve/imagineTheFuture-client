import React from 'react';
import { Header, SocialBar } from '../../components';

const Blog = () => {
    return (
        <>
            <Header/>
            <SocialBar/>
            <h1>BLOG</h1>
        </>
    );
};

export default Blog;