import React from 'react';
import { Header, SocialBar } from '../../components';

const About = () => {
    return (
        <>
            <Header/>
            <SocialBar/>
            <h1>ABOUT</h1>
        </>
    );
};

export default About;