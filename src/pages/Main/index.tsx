import React from 'react';
import { Header, SocialBar } from '../../components';

const Main = () => {
    return (
        <>
            <Header/>
            <SocialBar/>
            <h1>MAIN</h1>
        </>
    );
};

export default Main;